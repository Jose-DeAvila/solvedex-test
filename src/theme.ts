import { DefaultTheme } from 'styled-components';

export const defaultTheme: DefaultTheme = {
  borderRadius: '12px',
  palette: {
    primary: '#1B262C',
    secondColor: '#24343D',
    thirdColor: '#24353E',
    textColor: '#FFFFFF',
  }
}